# FoxPlan — optional fallback Google Maps API key for managed deployments.
# Users normally enter their own key at runtime via Settings → Google Maps.
# Never commit a real key. Copy this file to `.env.local` and set the value there.
VITE_GOOGLE_MAPS_API_KEY=
